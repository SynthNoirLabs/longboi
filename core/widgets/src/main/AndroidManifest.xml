<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- BIND_APPWIDGET is a system permission that must be requested at runtime
       via AppWidgetManager.bindAppWidgetIdIfAllowed() which prompts the user -->
</manifest>
